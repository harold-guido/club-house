<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <%= render 'feed' %>
      <% if user_signed_in? %>
        <%= render 'user_feed' %>
      <% end %>
    </div>
  </div>
</section>
